#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double a=3.14;
    double b =2;
    double c;

    // c = max(a,b);
    // c = min(a,b);
    // c  = pow(2,4);---- 16
    // c = sqrt(9);
    // c = abs(-9)
    //round off
    // c = round(a);-- int number 
    //c = ceil(a);-- uper bound
    // c = floor(a);--- lower bound


    
    return 0;
}